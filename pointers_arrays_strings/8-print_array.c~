#include"main.h"
/**
 *print_array - Function prints n elements of an arrays
 *@a: array of int
 *@n: number of elements
 *Return: nothing
 **/
void print_array(int *a, int n)
{
int i;
for (i = 0; i < n; i++)
{
printf("%d",a[i]);
if (i != (n -1))
{
printf(", ");
}
}
printf("\n");
}
