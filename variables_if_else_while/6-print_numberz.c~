#include <stdio.h>
/**
*main entry
*
*
*the function print all the single digit numbers of base 10
*
*
*/
int main(void)
{
int nb;
for (nb = 0; nb <= 9; nb++)
{
putchar('0' + nb);
}
putchar('\n');
return (0);
}
