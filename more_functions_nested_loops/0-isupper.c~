#include"main.h"
/**
 *_isupper - check if a character is upper
 *@c: is the checked number
 *Return: 1 for upper character or 0 for others
 **/
int _isupper(int c){
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
