#include"main.h"
/**
 *print_most_numbers - print the numbers since 0 up to 9 including 2 and 4
 *Return: the numbers
 **/
void print_most_numbers(void)
{
int i;
for(i =0; i <= 9; i++)
{
if (i == 2 || i == 4)
{
continue;
}
else
{
putchar(i + 'O');
}
}
putchar('\n');
}
